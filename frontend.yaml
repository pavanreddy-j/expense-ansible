- name: congigure nginx server
  hosts: frontend
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.dnf:
      name: nginx
      state: installed

  - name: start and enable nginx
    ansible.builtin.service:
      name: nginx
      state: present
      enablend: yes

  